# Simple example

/// @example
///  1 + 1 = 2

==>

Sassdoc(Example(Annotation, ExampleCode))

# Multi-line example

/// @example
///   @if foo {
///      @return bar;
///    }

==>

Sassdoc(Example(Annotation, ExampleCode))

