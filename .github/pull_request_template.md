<!--
Thank you for opening a PR 🌟

Before you continue, please:
  - set the target repo to `wkillerud/vscode-scss`
  - set the target branch to `main`
  - confirm `npm run lint && npm run test && npm run test:e2e && npm run test:web` runs without errors on your machine

If your PR has more than one commit, please set a descriptive title and summarize the changes.
If it's just one commit the default title is fine.
-->
